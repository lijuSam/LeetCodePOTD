// Maximum Product Difference Between Two Pairs 

// Intution is very simple just sort the array and take out first min , second min and first max and second max element this will be the Maximum Product Difference Between Two Pairs 

class Solution {
public:
    int maxProductDifference(vector<int>& nums) {

        sort(nums.begin(),nums.end());

        int product = 0;
        int i =0;
        int n = nums.size();

       product =   (nums[n-1] * nums[n-2]) - (nums[i]*nums[i+1]);


       return product;

        
    }
};
